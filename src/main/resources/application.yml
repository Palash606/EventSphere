server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eventsphere
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      jakarta:
        persistence:
          validation:
            mode: none
    open-in-view: false
  security:
    user:
      name: test
      password: test